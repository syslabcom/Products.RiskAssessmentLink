RiskAssessmentLink LMS Retriever    
================================

    Here is how to use it:
        
    
        >>> _ = self.folder.invokeFactory('RiskAssessmentLink', 'myral')
        >>> myob = getattr(self.folder, 'myral')
        >>> myob.setDescription('This is <a href="google.com">a link</a>.')
        >>> myob.setRemoteUrl('http://osha.europa.eu')
        >>> retrieve_manager = self.portal.portal_linkchecker.retrieving
           
    Now we can go on
    
        >>> retriever = retrieve_manager._getRetrieverForObject(myob)
        >>> links = retriever.retrieveLinks(myob)
        >>> 'google.com' in links
        True
        >>> 'http://osha.europa.eu' in links
        True
                
    Done